defmodule FoodFromHome.KafkaAgent.Producers do
  @moduledoc """
  Kafka producer functions.
  """
  # defp produce_menu_viewed_message(menu_id) do
  #   Kaffe.Producer.produce_sync("menu_viewed", [{:menu_id, menu_id}])
  # end
  #To be updated to
  # def produce_menu_viewed_message(menu_id, user_id) do
  #   Kaffe.Producer.produce_sync("menu_viewed", [{:menu_id, menu_id}, {:user_id, user_id}])
  # end
end
